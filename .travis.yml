language: node_js
node_js:
  - 6.10.1
before_install:
  - chmod +x ./Travis_Scripts/cf_setups.sh
  - sudo add-apt-repository -y ppa:ubuntu-toolchain-r/test
  - sudo apt-get -qq update
  - sudo apt-get -qq install g++-4.8
sudo: required
env:
  - CXX=g++-4.8
script: ./Travis_Scripts/cf_setups.sh